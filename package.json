{
  "name": "react-mapbox-deck-gl-geocoder",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14"
  },
  "cacheDirectories": [
    "node_modules",
    ".next/cache"
  ],
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "git add . && git commit -m Build && git push && git push heroku && heroku logs --tail --remote heroku"
  },
  "dependencies": {
    "deck.gl": "^8.3.10",
    "mapbox": "^1.0.0-beta10",
    "prop-types": "^15.7.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-map-gl": "^5.2.10",
    "react-scripts": "4.0.1",
    "serve": "^13.0.2",
    "viewport-mercator-project": "^7.0.1"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
